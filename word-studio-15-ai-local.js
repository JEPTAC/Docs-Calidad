/* ===== V83 · MOTOR IA LOCAL PROFESIONAL =====================================
   Motor gratuito y privado con WebLLM.
   - Solo usa modelos precompilados oficialmente compatibles.
   - Verifica inferencia real antes de declarar "IA lista".
   - Streaming, contexto documental reducido y conversación corta para menor latencia.
   - Fallback documental útil cuando no hay modelo.
   NO modifica la plantilla SGC base.
============================================================================= */
const EI_AI_BLUE='#001F73';
const EI_AI_YELLOW='#EAC800';
const EI_AI_WEBLLM_URL='https://esm.run/@mlc-ai/web-llm@0.2.85';
const EI_AI_MODELS=[
  {id:'SmolLM2-360M-Instruct-q4f32_1-MLC',label:'Rápida',detail:'SmolLM2 360M · menor consumo',profile:'fast'},
  {id:'Llama-3.2-1B-Instruct-q4f16_1-MLC',label:'Equilibrada',detail:'Llama 3.2 1B · recomendada',profile:'balanced'},
  {id:'Hermes-3-Llama-3.2-3B-q4f16_1-MLC',label:'Calidad',detail:'Hermes 3 / Llama 3.2 3B · equipo potente',profile:'quality'}
];
const EI_AI=window.EI_AI=window.EI_AI||{};
Object.assign(EI_AI,{engine:EI_AI.engine||null,worker:EI_AI.worker||null,webllm:EI_AI.webllm||null,status:EI_AI.status||'idle',model:EI_AI.model||EI_AI_MODELS[1].id,progress:EI_AI.progress||0,progressText:EI_AI.progressText||'Modelo no cargado',imported:EI_AI.imported||null,lastResult:EI_AI.lastResult||'',lastAction:EI_AI.lastAction||'',history:Array.isArray(EI_AI.history)?EI_AI.history:[],selection:EI_AI.selection||null,busy:false,verified:!!EI_AI.verified,metrics:EI_AI.metrics||{},generationId:0,supportedModels:EI_AI.supportedModels||[]});
function eiAiEscape(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function eiAiPlain(v){const d=document.createElement('div');d.innerHTML=String(v||'');return(d.innerText||d.textContent||'').replace(/\u00a0/g,' ').replace(/[ \t]+\n/g,'\n').trim()}
function eiAiNormalizeText(text){return String(text||'').replace(/\r/g,'').replace(/[ \t]{2,}/g,' ').replace(/[ \t]+\n/g,'\n').replace(/\n{4,}/g,'\n\n\n').trim()}
function eiAiChunkText(text,maxChars=1800,overlap=120){const src=eiAiNormalizeText(text);if(!src)return[];const paras=src.split(/\n{2,}/).map(x=>x.trim()).filter(Boolean),out=[];let buf='';const push=()=>{if(!buf.trim())return;out.push({id:out.length,text:buf.trim()});buf=buf.slice(Math.max(0,buf.length-overlap))};for(const p of paras){if(buf&&buf.length+p.length+2>maxChars)push();buf+=(buf?'\n\n':'')+p;if(buf.length>maxChars*1.25)push()}if(buf.trim())out.push({id:out.length,text:buf.trim()});return out}
const EI_AI_STOP=new Set('de la el los las un una unos unas y o e u a ante bajo con contra desde durante en entre hacia hasta mediante para por según sin sobre tras que del al se su sus es son fue fueron ser como más menos muy ya si no lo le les este esta estos estas ese esa esos esas'.split(' '));
function eiAiTerms(text){return[...new Set(String(text||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').match(/[a-z0-9]{3,}/g)||[])].filter(x=>!EI_AI_STOP.has(x)).slice(0,24)}
function eiAiRetrieve(query,chunks,limit=3){const terms=eiAiTerms(query);if(!terms.length)return(chunks||[]).slice(0,limit);return(chunks||[]).map(c=>{const low=c.text.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');let score=0;for(const t of terms){const n=low.split(t).length-1;score+=Math.min(n,4)*(t.length>6?2:1)}return{...c,score}}).sort((a,b)=>b.score-a.score||a.id-b.id).slice(0,limit)}
function eiAiBlockText(b){if(!b)return'';if(['text','callout'].includes(b.type))return b.text||eiAiPlain(b.richText||'');if(b.type==='heading')return b.text||'';if(b.type==='list')return(b.items||[]).join('\n');if(b.type==='table')return(b.rows||[]).map(r=>r.join(' | ')).join('\n');if(b.type==='chart')return`${b.title||'Gráfica'}\n${(b.labels||[]).map((x,i)=>`${x}: ${(b.values||[])[i]??''}`).join('\n')}`;if(b.type==='kpi')return(b.items||[]).map(x=>`${x.label}: ${x.value} ${x.detail||''}`).join('\n');if(b.type==='diagram')return`${b.title||'Diagrama'}\n${(b.nodes||[]).map(x=>x.text||x.label||'').join('\n')}`;return''}
function eiAiCurrentDocumentText(){if(typeof doc==='undefined'||!Array.isArray(doc.sections))return'';const out=[];doc.sections.forEach((s,i)=>{const n=s.n||i+1;out.push(`${n} ${s.t||''}`.trim());if(s.c)out.push(String(s.c));(s.sub||[]).forEach((ss,j)=>{out.push(`${ss.n||`${n}.${j+1}`} ${ss.t||''}`.trim());if(ss.c)out.push(String(ss.c));(ss.blocks||[]).forEach(b=>out.push(eiAiBlockText(b)))});(s.blocks||[]).forEach(b=>out.push(eiAiBlockText(b)))});return eiAiNormalizeText(out.filter(Boolean).join('\n\n'))}
function eiAiSelectedText(){const sel=window.getSelection?.();if(!sel||!sel.rangeCount||sel.isCollapsed)return'';const r=sel.getRangeAt(0),el=r.commonAncestorContainer.nodeType===1?r.commonAncestorContainer:r.commonAncestorContainer.parentElement;const editor=el?.closest?.('.word-rich-editor');if(!editor)return'';EI_AI.selection={editor,range:r.cloneRange()};return sel.toString().trim()}
function eiAiSourceText(query=''){const selected=eiAiSelectedText();if(selected)return{kind:'selection',text:selected.slice(0,5200),label:'texto seleccionado'};const base=EI_AI.imported?.text||eiAiCurrentDocumentText();if(!base)return{kind:'none',text:'',label:'sin contexto'};const chunks=EI_AI.imported?.chunks||eiAiChunkText(base);const picked=query?eiAiRetrieve(query,chunks,3):chunks.slice(0,3);return{kind:EI_AI.imported?'imported':'current',text:picked.map(x=>x.text).join('\n\n---\n\n').slice(0,5200),label:EI_AI.imported?.name||'documento actual'}}
function eiAiSetStatus(status,text,progress=null){EI_AI.status=status;EI_AI.progressText=text||status;if(progress!==null)EI_AI.progress=Math.max(0,Math.min(1,Number(progress)||0));if(typeof eiAiRefreshStatus==='function')eiAiRefreshStatus()}
function eiAiHardware(){return{webgpu:!!navigator.gpu,wasm:typeof WebAssembly!=='undefined',secure:window.isSecureContext!==false}}
function eiAiResetConversation(){EI_AI.history=[];EI_AI.lastResult='';EI_AI.lastAction='';EI_AI.generationId++}
function eiAiHistoryForModel(){return(EI_AI.history||[]).filter(x=>x&&['user','assistant'].includes(x.role)&&x.text).slice(-4).map(x=>({role:x.role,content:String(x.text).slice(0,1600)}))}
function eiAiRemember(role,text){const clean=String(text||'').trim();if(!clean)return;EI_AI.history.push({role,text:clean,at:Date.now()});if(EI_AI.history.length>10)EI_AI.history=EI_AI.history.slice(-10)}
async function eiAiLoadRuntime(){const webllm=EI_AI.webllm||(EI_AI.webllm=await import(EI_AI_WEBLLM_URL));const supported=(webllm.prebuiltAppConfig?.model_list||[]).map(x=>x.model_id);EI_AI.supportedModels=supported;return webllm}
function eiAiCompatibleModel(requested,supported=[]){if(supported.includes(requested))return requested;for(const m of EI_AI_MODELS)if(supported.includes(m.id))return m.id;const llama=supported.find(x=>/Llama-3\.2-1B-Instruct-q4f16_1-MLC/.test(x));if(llama)return llama;throw new Error('La versión de WebLLM cargada no ofrece ninguno de los modelos configurados. Actualiza la página y vuelve a intentarlo.')}
async function eiAiCreateEngine(webllm,modelId,onProgress){const opts={initProgressCallback:onProgress,logLevel:'WARN'};if(typeof webllm.CreateWebWorkerMLCEngine==='function'&&typeof Worker!=='undefined'){try{const workerCode=`import * as webllm from ${JSON.stringify(EI_AI_WEBLLM_URL)};const handler=new webllm.WebWorkerMLCEngineHandler();self.onmessage=(msg)=>handler.onmessage(msg);`;const url=URL.createObjectURL(new Blob([workerCode],{type:'text/javascript'}));const worker=new Worker(url,{type:'module'});EI_AI.worker=worker;const engine=await webllm.CreateWebWorkerMLCEngine(worker,modelId,opts);URL.revokeObjectURL(url);return engine}catch(err){try{EI_AI.worker?.terminate()}catch{}EI_AI.worker=null;console.warn('WebLLM worker fallback:',err)}}return webllm.CreateMLCEngine(modelId,opts)}
async function eiAiVerifyEngine(engine){eiAiSetStatus('verifying','Verificando respuesta real del modelo…',1);const test=await engine.chat.completions.create({messages:[{role:'system',content:'Responde exactamente con la palabra OK.'},{role:'user',content:'Prueba de funcionamiento.'}],temperature:0,max_tokens:8,stream:false});const answer=String(test?.choices?.[0]?.message?.content||'').trim();if(!answer)throw new Error('El modelo se descargó pero no respondió la prueba de inferencia.');return answer}
async function eiAiLoadModel(modelId=EI_AI.model){if(EI_AI.busy)return EI_AI.engine;const hw=eiAiHardware();if(!hw.webgpu)throw new Error('Este navegador o equipo no expone WebGPU. La lectura, organización y análisis estructural siguen funcionando, pero el chat generativo local necesita WebGPU.');EI_AI.busy=true;EI_AI.verified=false;eiAiSetStatus('loading','Preparando WebLLM…',0);const started=performance.now();try{const webllm=await eiAiLoadRuntime();const compatible=eiAiCompatibleModel(modelId,EI_AI.supportedModels);EI_AI.model=compatible;const onProgress=r=>eiAiSetStatus('loading',r.text||'Descargando modelo…',r.progress??EI_AI.progress);EI_AI.engine=await eiAiCreateEngine(webllm,compatible,onProgress);await eiAiVerifyEngine(EI_AI.engine);EI_AI.verified=true;EI_AI.metrics.loadMs=Math.round(performance.now()-started);eiAiSetStatus('ready','IA verificada y lista para conversar',1);return EI_AI.engine}catch(err){try{EI_AI.worker?.terminate()}catch{}EI_AI.worker=null;EI_AI.engine=null;EI_AI.verified=false;eiAiSetStatus('error',String(err?.message||err),0);throw err}finally{EI_AI.busy=false}}
function eiAiSystemPrompt(){return `Eres un asistente documental profesional que trabaja localmente para ELECTROINGENIERÍA S.A.S. Responde en español claro, natural, técnico y útil. Tu prioridad es producir una respuesta que el usuario pueda aplicar directamente al documento. REGLAS OBLIGATORIAS: 1) no inventes normas, fuentes, autores, cifras, responsables ni evidencias; 2) separa hechos detectados de recomendaciones; 3) si falta información, indica exactamente qué falta; 4) cuando corrijas o reescribas, conserva datos y significado; 5) usa terminología consistente; 6) evita relleno, frases genéricas y introducciones innecesarias; 7) si existe contexto documental, responde primero con base en él; 8) si el usuario pide una mejora, entrega la versión mejorada y luego, solo si aporta valor, explica cambios; 9) para auditorías produce hallazgos concretos y accionables; 10) sé conciso por defecto para responder más rápido.`}
async function eiAiGenerate(instruction,{context='',temperature=.15,maxTokens=420,onUpdate=null,conversation=false}={}){if(!EI_AI.engine||!EI_AI.verified)throw new Error('La IA local todavía no está verificada. Cárgala en “Modelo” y espera el estado “IA verificada y lista”.');const userText=String(instruction||'').trim(),messages=[{role:'system',content:eiAiSystemPrompt()}];if(context)messages.push({role:'system',content:`CONTEXTO DOCUMENTAL RELEVANTE:\n---\n${String(context).slice(0,5200)}\n---\nNo inventes información fuera de este contexto cuando la respuesta dependa del documento.`});if(conversation)messages.push(...eiAiHistoryForModel());messages.push({role:'user',content:userText});EI_AI.busy=true;const generation=++EI_AI.generationId;eiAiSetStatus('thinking','Redactando…',1);const start=performance.now();let firstTokenAt=0;try{let text='';const stream=await EI_AI.engine.chat.completions.create({messages,temperature,top_p:.9,max_tokens:maxTokens,stream:true});for await(const chunk of stream){if(generation!==EI_AI.generationId)break;const delta=chunk?.choices?.[0]?.delta?.content||'';if(delta&&!firstTokenAt)firstTokenAt=performance.now();text+=delta;if(onUpdate)onUpdate(text,{streaming:true})}text=text.trim();if(!text)throw new Error('El modelo no produjo contenido. Prueba nuevamente o cambia de perfil.');EI_AI.lastResult=text;EI_AI.metrics.lastMs=Math.round(performance.now()-start);EI_AI.metrics.firstTokenMs=firstTokenAt?Math.round(firstTokenAt-start):null;if(conversation){eiAiRemember('user',userText);eiAiRemember('assistant',text)}else eiAiRemember('assistant',text);eiAiSetStatus('ready','IA verificada y lista',1);if(onUpdate)onUpdate(text,{streaming:false});return text}catch(err){eiAiSetStatus('error',String(err?.message||err),1);throw err}finally{EI_AI.busy=false}}
function eiAiFallbackClean(text){let s=String(text||'');const fixes=[[/\bqeu\b/gi,'que'],[/\bporfavor\b/gi,'por favor'],[/\btambien\b/gi,'también'],[/\besta\s+bien\b/gi,'está bien'],[/\besta\s+mal\b/gi,'está mal']];fixes.forEach(([r,v])=>s=s.replace(r,v));return s.replace(/[ \t]+([,.;:!?])/g,'$1').replace(/([,.;:!?])([^\s\n”"')\]])/g,'$1 $2').replace(/[ \t]{2,}/g,' ').replace(/\n[ \t]+/g,'\n').trim()}
function eiAiFallbackSummary(text){const s=eiAiNormalizeText(text),sentences=s.match(/[^.!?\n]+[.!?]?/g)||[];if(sentences.length<=5)return s;const terms=eiAiTerms(s);return sentences.map((x,i)=>({x,i,score:terms.reduce((a,t)=>a+(x.toLowerCase().includes(t)?1:0),0)+(i===0?1:0)})).sort((a,b)=>b.score-a.score).slice(0,Math.min(7,Math.max(3,Math.ceil(sentences.length*.2)))).sort((a,b)=>a.i-b.i).map(x=>x.x.trim()).join(' ')}
function eiAiFallbackDocumentAnswer(q){const source=eiAiSourceText(q);if(!source.text)return'No encuentro contenido para analizar. Carga un archivo o escribe contenido en el documento.';const query=String(q||'').toLowerCase();const imported=EI_AI.imported;if(imported?.analysis&&/(estructura|titulo|título|seccion|sección|organiza|organizar|que falta|qué falta)/i.test(query)){const a=imported.analysis,heads=(imported.blocks||[]).filter(b=>b.enabled!==false&&b.type==='heading').slice(0,18);return`Analicé la estructura de ${imported.name}. Detecté ${heads.length} títulos/subtítulos, ${a.tableProfiles?.length||0} tablas y una confianza aproximada de ${a.confidence||0}%.\n\nJerarquía detectada:\n${heads.map(h=>`${'  '.repeat(Math.max(0,(h.level||1)-1))}• Nivel ${h.level||1}: ${h.text}`).join('\n')||'• No se identificaron encabezados claros.'}\n\nPuedes ajustar cada bloque en la pestaña Archivo antes de importarlo: cambiar entre título, subtítulo, párrafo o lista; moverlo; editar su texto; o excluirlo.`}const chunks=eiAiRetrieve(q,eiAiChunkText(source.text),3);if(!chunks.length)return'No encontré un fragmento claramente relacionado con esa pregunta.';return`Encontré estos fragmentos directamente relacionados en ${source.label}:\n\n${chunks.map((c,i)=>`${i+1}. ${c.text}`).join('\n\n')}\n\nPara una interpretación y redacción completa, carga la IA local desde “Modelo”.`}
function eiAiQuickResult(action,text){if(action==='correct')return eiAiFallbackClean(text);if(action==='summary')return eiAiFallbackSummary(text);if(action==='structure'){const st=typeof eiAiInferStructure==='function'?eiAiInferStructure(text):[];return st.length?st.map((x,i)=>`${i+1}. ${x.title}\n${x.content||''}`).join('\n\n'):text}return''}
async function eiAiRunAction(action,customPrompt='',onUpdate=null){const labels={correct:'Corrección ortográfica',rewrite:'Mejora de redacción',summary:'Resumen',structure:'Organización',formal:'Redacción formal',audit:'Auditoría documental',table:'Propuesta de tabla',flow:'Flujograma',concept:'Mapa conceptual'},source=eiAiSourceText(customPrompt||labels[action]||'documento');if(!source.text)throw new Error('No hay texto disponible. Selecciona un fragmento, importa un archivo o abre un documento con contenido.');if((!EI_AI.engine||!EI_AI.verified)&&['correct','summary','structure'].includes(action)){const result=eiAiQuickResult(action,source.text);EI_AI.lastResult=result;EI_AI.lastAction=action;if(onUpdate)onUpdate(result,{streaming:false});return result}if(!EI_AI.engine||!EI_AI.verified)throw new Error('Esta herramienta requiere la IA generativa. Carga y verifica un modelo local desde “Modelo”.');const prompts={correct:'Corrige ortografía, tildes, concordancia, puntuación y gramática. Mantén exactamente los datos y el sentido. Devuelve únicamente el texto corregido.',rewrite:'Reescribe con calidad profesional: clara, natural, precisa y cohesionada. Elimina redundancias y frases sueltas. No inventes información. Devuelve primero la versión mejorada.',summary:'Resume conservando decisiones, cifras, responsables, restricciones y conclusiones importantes. No agregues información externa.',structure:'Reorganiza el contenido con una jerarquía lógica de títulos y subtítulos. Conserva toda información útil e indica qué contenido pertenece a cada encabezado.',formal:'Reescribe con tono institucional y técnico, claro y humano. Evita grandilocuencia, muletillas y lenguaje artificial.',audit:'Audita rigurosamente el documento. Entrega: HALLAZGOS, VACÍOS DE INFORMACIÓN, INCONSISTENCIAS, DATOS QUE REQUIEREN SOPORTE y RECOMENDACIONES ACCIONABLES. No inventes requisitos.',table:'Identifica información que conviene convertir en tabla. Devuelve título, columnas y filas usando | como separador. Solo usa datos existentes.',flow:'Convierte el proceso en un flujograma lógico. Una línea por nodo: [I] inicio, [P] proceso, [D] decisión, [DOC] documento, [F] fin. No inventes pasos.',concept:'Construye un mapa conceptual. Primera línea: concepto central. Siguientes: RELACIÓN | CONCEPTO. Usa relaciones breves basadas en el texto.'};EI_AI.lastAction=action;return eiAiGenerate(customPrompt||prompts[action]||'Ayuda a mejorar este contenido.',{context:source.text,maxTokens:action==='audit'?600:400,onUpdate,conversation:false})}
window.EI_AI_CORE_READY=true;window.EI_AI_V83_READY=true;